package dk.mk.bee.util;

import dk.mk.bee.game.config.GameInfo;
import dk.mk.bee.ui.config.VisualInfo;

public class MapUtils {
    /* //TODO should be able to be deleted
    public static int getTileMapHeight() {
        //return (VisualInfo.WINDOW_HEIGHT / GameInfo.SQUARE_SIZE) - 1;
        return (VisualInfo.WINDOW_HEIGHT / GameInfo.SQUARE_SIZE);
    }

    public static int getTileMapWidth() {
        //return (VisualInfo.WINDOW_WIDTH / GameInfo.SQUARE_SIZE) - 1;
        return (VisualInfo.WINDOW_WIDTH / GameInfo.SQUARE_SIZE);
    }
     */
}
